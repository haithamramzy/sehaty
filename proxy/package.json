{
  "name": "sehaty-proxy",
  "version": "0.1.0",
  "private": true,
  "description": "MiniMax proxy for the صحّتي app — the app talks to this, only this server holds the real API key.",
  "main": "server.js",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.21.2"
  }
}
